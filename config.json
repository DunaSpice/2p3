{
  "debug": true,
  "models_list": ["gpt-3.5-turbo", "gpt-3.5-turbo-16k", "gpt-4"],
  "openai_model": "gpt-3.5-turbo",
  "exit_command": "exit",
  "system_message": "You mast find and do command shell only this OS.",
  "tool_import": "import subprocess",
  "tool_function": "def execute_command(command):\n    return subprocess.run(command, shell=True, capture_output=True, text=True, check=True).stdout.strip()",
  "tool_arg": {
    "type": "function",
    "function": {
      "name": "execute_command",
      "description": "Execute a system command line with shell subprocess module",
      "parameters": {
        "type": "object",
        "properties": {
          "command": {
            "type": "string",
            "description": "The command to execute"
          }
        },
        "required": [
          "command"
        ]
      }
    }
  }
}
